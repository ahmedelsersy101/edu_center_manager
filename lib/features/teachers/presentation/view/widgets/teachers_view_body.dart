import 'package:flutter/material.dart';

class TeachersViewBody extends StatelessWidget {
  const TeachersViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Teachers View Body'));
  }
}
